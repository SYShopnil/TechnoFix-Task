import Head from 'next/head';

import { LandingPageView } from '@src/components/view';
import { tableMockData } from '@root/__mock-props__/component';
import { ITableData } from '@src/types/view';
const Home = ({ homePageData }: { homePageData: ITableData }) => {
  return (
    <div>
      <Head>
        <title>TechnoFix Task</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <main>
        <LandingPageView {...homePageData} />
      </main>
    </div>
  );
};

export async function getStaticProps() {
  return {
    props: {
      homePageData: tableMockData,
    }, // will be passed to the page component as props
  };
}

export default Home;
